services:
  - type: web
    name: hugocars-qr
    env: docker
    plan: free
    disk:
      name: cardata
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: DATA_DIR
        value: /data
      - key: PORT
        value: 8080
